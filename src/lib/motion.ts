// Shared easing curve for the marketing animations.
// Typed as a 4-tuple so Framer Motion accepts it as a cubic-bezier.
export const EASE_OUT: [number, number, number, number] = [0.22, 1, 0.36, 1];
